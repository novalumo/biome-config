name: Release

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install Dependencies
        run: pnpm install

      - name: Create Release Pull Request
        id: changesets
        uses: changesets/action@v1
        with:
          version: pnpm run version
          createGithubReleases: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if version is published
        id: check
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if npm view @novalumo/biome-config@$VERSION 2>/dev/null; then
            echo "published=true" >> $GITHUB_OUTPUT
          else
            echo "published=false" >> $GITHUB_OUTPUT
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Debug OIDC environment
        if: steps.check.outputs.published == 'false'
        run: |
          echo "npm version: $(npm --version)"
          echo "ACTIONS_ID_TOKEN_REQUEST_URL: ${ACTIONS_ID_TOKEN_REQUEST_URL:-not set}"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN length: ${#ACTIONS_ID_TOKEN_REQUEST_TOKEN}"
          cat ~/.npmrc 2>/dev/null || echo "No .npmrc"
          env | grep -i oidc || echo "No OIDC env vars"
          env | grep -i actions || echo "No ACTIONS env vars"

      - name: Publish to npm with OIDC
        if: steps.check.outputs.published == 'false'
        run: npm publish --provenance --access public --loglevel verbose 2>&1 | head -100
        env:
          NPM_CONFIG_PROVENANCE: true
          DEBUG: "*"

      - name: Create GitHub Release
        if: steps.check.outputs.published == 'false'
        run: |
          gh release create "v${{ steps.check.outputs.version }}" \
            --title "v${{ steps.check.outputs.version }}" \
            --generate-notes \
            || echo "Release already exists or failed"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
